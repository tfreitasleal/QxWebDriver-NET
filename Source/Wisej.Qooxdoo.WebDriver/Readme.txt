The Wisej.Qooxdoo.WebDriver.Debug runs in Debug configuration only.
It uses a DEBUGJS conditional compiler constant and relies on the web application to load all the scripts.

To make the Javascript debugger stop, use
debugger;

This feature isn't supported in Chrome